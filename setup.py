"""Setup script for editable installs.

This file exists primarily to support `pip install -e .` for development.
As of setuptools 68+, pyproject.toml is the primary configuration source.
"""
from setuptools import setup

setup()
